{
  "name": "obsidian-persistent-graph",
  "version": "2.0.0",
  "description": "Obsidian plugin: save & switch named Graph View presets",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "lint": "eslint src/"
  },
  "devDependencies": {
    "obsidian": "latest",
    "typescript": "^5.4",
    "esbuild": "^0.19",
    "@types/node": "^20",
    "tslib": "^2.6"
  }
}
